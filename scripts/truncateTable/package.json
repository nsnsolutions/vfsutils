{
  "name": "truncateTable",
  "version": "1.0.0",
  "description": "Remov a dynamo table.",
  "bin": {
    "truncateTable": "./bin/truncateTable.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "argparse": "^1.0.9",
    "async": "^2.1.2",
    "aws-sdk": "^2.6.10",
    "sleep": "^5.0.0"
  }
}
